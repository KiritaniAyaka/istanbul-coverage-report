{
  "scripts": {
    "test": "vitest --coverage",
    "clean": "rimraf coverage .nyc_output"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^0.23.4",
    "rimraf": "^3.0.2",
    "vitest": "^0.23.4"
  }
}
